# Dependencies
node_modules/

# Build output
dist/
build/

# Verification / scratch screenshots (regenerated locally)
**/docs/screenshots/
**/shots/

# Logs
*.log
npm-debug.log*

# Env / local
*.local
.env
.env.*
!.env.example
share-link.url

# OS / editor cruft
.DS_Store

# Supabase CLI local cache — leaks the project/org ref, never commit it.
# (No longer relevant anyway: Supabase itself was fully removed from this
# codebase — see backend/src/store.ts — but the CLI can still regenerate
# this folder locally if anyone runs `supabase link` again.)
**/supabase/.temp/
